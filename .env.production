# 生产环境中，设置后端服务URL为实际部署的后端服务URL。
# Render、Nginx、云负载均衡等通常负责 HTTPS，Node 服务本身监听 HTTP 端口。
REACT_APP_SERVER_URL=https://gomoku-game-backend.onrender.com

# 注意：在部署前，请确保上面的URL与您在服务器上部署的后端服务URL一致
# 如果您的服务器配置了HTTPS或反向代理，请使用https://而不是http://

# 如果您有域名，可以使用域名代替IP地址，例如：
# REACT_APP_SERVER_URL=https://api.yourdomain.com

# 如果您还没有部署服务器，可以使用以下服务：
# - Render (https://render.com)
# - Railway (https://railway.app)
# - Fly.io (https://fly.io)
